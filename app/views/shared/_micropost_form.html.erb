<%= form_for(@micropost) do |f| %>
<<<<<<< HEAD

<%= render 'shared/error_messages', object: f.object %>

  <div class="field">
<%= f.text_area :content, placeholder: "Compose new micropost..." %>
  </div>
<%= f.submit "Post", class: "btn btn-primary" %>

=======
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
  <%= f.text_area :content, placeholder:"Let's tweet!" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
>>>>>>> user-profile
<% end %>